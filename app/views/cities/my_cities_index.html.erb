<h1>Your Cities</h1>

<div class="cities-container">
  <% @cities.each do |city| %>
  <div>
    <p><%= city.name %></p>
    <a href="/mycities/<%= city.id %>/attractions"><img class="resize" src="<%= city.photo_url %>"></a>
  </div>
  <% end %>
</div>
